#include <bits/stdtr1c++.h>
using namespace std;


int main() {
    int t, n, k;
    cin >> t;

    while (t--) {
        cin >> n >> k;
        vector<int> vt(n, 0);
        deque<int> a;
        vector<int> rs;
        
    }
    
    return 0;
}